// Test 1: Simple function - basic sanity check
function processData(input) {
    const result = input * 2;
    return result;
}

// Another function to ensure we don't get confused
function unrelated() {
    return "not this one";
}
