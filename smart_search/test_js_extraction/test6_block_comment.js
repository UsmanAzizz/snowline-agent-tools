// Test 6: Braces inside a block comment spanning multiple lines
function blockComment(value) {
    /*
     * This is a multi-line comment
     * with {nested} braces inside
     * and more {stuff} here
     */
    return value * 2;
}
